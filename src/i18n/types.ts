export type Translations = {
	navbar: Navbar;
	hero: Hero;
};

export type Navbar = {
	projects: string;
	skills: string;
	occupations: string;
};

export type Hero = {
	role: string;
	bio: string;
	contact: string;
};
